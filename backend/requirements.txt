fastapi
uvicorn
python-multipart
pydantic
pydantic-settings
numpy
# tensorflow[and-cuda] self-bundles CUDA 12.3 + cuDNN 8 as pip wheels —
# no system CUDA install needed inside WSL. Your Windows NVIDIA driver
# (561.17) provides the GPU; WSL2 passes it through automatically.
# RTX 4050 (Ada, compute capability 8.9) is fully supported.
tensorflow[and-cuda]==2.17.1
opencv-python-headless
librosa
soundfile
scikit-learn
pillow
requests
python-dotenv
pytest
pytest-asyncio
httpx
ollama
aiohttp
# tflite-runtime

# Add these to your existing requirements.txt
python-dotenv>=1.0.0
kaggle>=1.6.0
huggingface_hub>=0.20.0
httpx>=0.27.0